spring:
  application:
    name: toy
  datasource:
    url: jdbc:sqlite:toy-sqlite.db
    driver-class-name: org.sqlite.JDBC
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: update #none, create(?? ??), create-drop(???, ?????? ?? ? ??), update(??? ??, ???? ??), validate(entity? ?????? ??? ?? ??)